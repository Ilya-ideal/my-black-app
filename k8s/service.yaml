apiVersion: v1
kind: Service
metadata:
  name: my-black-app-service
  namespace: my-black-app
spec:
  selector:
    app: my-black-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer